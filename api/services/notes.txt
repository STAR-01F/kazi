1. Added Firebase dependency via npm install Firebase
2. For federated auth services my email (tlawal86@gmail.com) is being used as a failsafe, this
   would need to be changed to a group STARS email if/when we have one
